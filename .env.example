# Environment Variables for PMP Progress Tracker
# Copy this to .env.local for local development

# Vercel Postgres Database (for production)
# Get these from your Vercel dashboard after creating a Postgres database
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NO_SSL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# Optional: Admin password
# ADMIN_PASSWORD=your-secure-password-here

# Optional: Override database type (auto-detects based on POSTGRES_URL)
# DATABASE_TYPE=sqlite | postgres